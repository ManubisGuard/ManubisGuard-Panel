# AI Handoff — 2026-09-25

## Current state
Clean branch image and installer/runtime gates were executed on the new disposable server.

### PASS
- Clean branch Docker image build.
- `dashboard/build/index.html` exists in the built image.
- PWA generation.
- `docker compose config`.
- `bash -n` installer/migration scripts.
- Discovery regression.
- Feature-scope Ruff lint/format.
- Full `scripts/run-local-tests.sh` in disposable test copy: exit 0; 86 migration tests; bridge counts and CAGG matched.
- Exact installer execution: `bash install-manubisguard.sh install --database timescaledb --yes` exit 0.
- Installed Panel import.
- Installed Panel HTTP health.
- Panel and TimescaleDB healthy after installer deployment.

## Current blocker
The required real backup `/root/backup_20260923210118.zip` is not present on the new server, and no replacement backup was found under the inspected local test data paths. Per project rules it must not be synthesized or replaced from GitHub.

Therefore the following gates are intentionally NOT marked PASS on this server:
1. Real staging restore from the specified backup.
2. Full post-restore validation.
3. Repeat bridge/upgrade gate tied to that restore.
4. Real rollback based on that restore.
5. Production cutover readiness.

## Infrastructure note
The new VPS has a restrictive Docker bridge forwarding policy. The installer/build test temporarily used a local Docker wrapper to add `--network=host` to build commands. The wrapper, temporary daemon config and temporary firewall rules were removed afterward. The repository source was not modified for this host networking condition.

## Safety
Production was not modified. No `--apply` or production cutover was executed. No secrets were committed.
